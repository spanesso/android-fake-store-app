# Módulo `:core:testing`

**Estado**: estructura vacía (ETAPA 0.5).
**Implementación prevista**: ETAPA 1.10.
**Propósito**: Utilidades de test compartidas (fakes, rules, dispatchers test, builders de DomainError/UiError, tests Konsist).

## Convention plugin

Aplica `mango.kotlin.library` desde `build-logic/`.

## Documentación detallada

Cuando el módulo se implemente, ver `core/testing/docs/` (generado por el skill
`documentar-modulo`).
