# Módulo `:core:network`

**Estado**: estructura vacía (ETAPA 0.5).
**Implementación prevista**: ETAPA 1.5.
**Propósito**: OkHttp/Retrofit base, interceptors, certificate pinning, NetworkErrorMapper, ConnectivityObserver, retry con backoff.

## Convention plugin

Aplica `mango.android.library` desde `build-logic/`.

## Documentación detallada

Cuando el módulo se implemente, ver `core/network/docs/` (generado por el skill
`documentar-modulo`).
