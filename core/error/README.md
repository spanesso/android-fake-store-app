# Módulo `:core:error`

**Estado**: estructura vacía (ETAPA 0.5).
**Implementación prevista**: ETAPA 1.2.
**Propósito**: DomainError sealed, UiError, ErrorMapper base, safeApiCall/safeDbCall, contratos de reportería.

## Convention plugin

Aplica `mango.kotlin.library` desde `build-logic/`.

## Documentación detallada

Cuando el módulo se implemente, ver `core/error/docs/` (generado por el skill
`documentar-modulo`).
