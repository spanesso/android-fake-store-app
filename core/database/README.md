# Módulo `:core:database`

**Estado**: estructura vacía (ETAPA 0.5).
**Implementación prevista**: ETAPA 1.6.
**Propósito**: Room base, encriptación SQLCipher, migrations, DatabaseErrorMapper.

## Convention plugin

Aplica `mango.android.library` desde `build-logic/`.

## Documentación detallada

Cuando el módulo se implemente, ver `core/database/docs/` (generado por el skill
`documentar-modulo`).
